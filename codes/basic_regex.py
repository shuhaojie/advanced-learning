import re

F_100369 = '(?P<F_100369>[\d\.\s]*?%)'
F_empty_30 = '.{0,30}?'  # 用于正则的空白字符匹配

s = re.compile(fr'投资者认购比例及出资额{F_empty_30}占增资后股比约(?P<F_100369>[\d\.\s]*?%)')
c = s.match('投资者认购比例及出资额&占增资后股比约')
print(c)